# Coding guidelines 

- Use the OpenSpec change process for all but trivial changes and additions to the system.
- Use Python, `uv`, and a `pyproject.toml` for utility code and scripts.  
- Prefer tools like Leaflet for the interactive map.


